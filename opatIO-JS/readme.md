# Simple JS parser for opat files
This is not super well maintained and is primarily intended to be used with the `index.html` file provided for a simple
opat browser.

# Usage

```javascript
opatFile = parseOPAT(await file.arrayBuffer());
 ```